name: test

env:
  prodution: '[{ "name": "apple", "quantity": 1 },{ "name": "orange", "quantity": 2 }]'
  myenv: production
on:
  push:
    branches:
      - 'main'
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: step-1
        run: |
          echo "${{ env[env.myenv] }}"
          echo 'g'
      
      
      
